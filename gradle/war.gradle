apply plugin: "war"

bootWar {
    mainClass = "com.icthh.xm.ms.mstemplate.MstemplateApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
